

public class Main{
    public static void main(String[] args) {
        // overloaded constructor = mutiple constructors with the same name but different set of parameters.
        // name+parameters (signature)    It should be different.

        Pizza p = new Pizza();
        p.show();
        
    }
}